meta {
  name: Get Prize
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/prizes/:id
  body: none
  auth: bearer
}

params:path {
  id: {{prizeGoBoardId}}
}

auth:bearer {
  token: {{token}}
}
