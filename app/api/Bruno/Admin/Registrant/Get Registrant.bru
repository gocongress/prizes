meta {
  name: Get Registrant
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/admin/registrants/:id
  body: none
  auth: bearer
}

params:path {
  id: {{testRegistrantId}}
}

auth:bearer {
  token: {{token}}
}
