meta {
  name: Delete Registrant
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/admin/registrants/:id
  body: none
  auth: bearer
}

params:path {
  id: {{testRegistrantId}}
}

auth:bearer {
  token: {{token}}
}
