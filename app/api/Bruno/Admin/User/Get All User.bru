meta {
  name: Get All User
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/admin/users?orderBy=createdAt&pageSize=100&orderDirection=asc
  body: none
  auth: bearer
}

params:query {
  orderBy: createdAt
  pageSize: 100
  orderDirection: asc
}

auth:bearer {
  token: {{token}}
}
