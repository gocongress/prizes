meta {
  name: Update User
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/v1/admin/users/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: {{testUser2Id}}
}

body:json {
  {
    "scope": "ADMIN"
  }
}
