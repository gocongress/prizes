meta {
  name: Welcome User Email
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/admin/users/welcome
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "agaId": "{{agaId}}"
  }
}

