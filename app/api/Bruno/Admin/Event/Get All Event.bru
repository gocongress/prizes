meta {
  name: Get All Event
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/admin/events?orderBy=title&pageSize=12&orderDirection=asc
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  orderBy: title
  pageSize: 12
  orderDirection: asc
}
