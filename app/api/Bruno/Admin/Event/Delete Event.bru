meta {
  name: Delete Event
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/admin/events/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: {{eventSpring2025Id}}
}
