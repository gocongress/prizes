meta {
  name: Create Player
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/admin/players
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "email": "{{user2Email}}",
    "agaId": "54321",
    "name": "New Player",
    "rank": -5.0
  }
}
