meta {
  name: Get Player
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/admin/players/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: {{player1Id}}
}
