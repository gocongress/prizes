meta {
  name: Sync
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/admin/players/sync
  body: json
  auth: bearer
}

auth:bearer {
  token: {{BADGEFILE_API_KEY}}
}

body:json {
  { 
    "agaId": "12298", 
    "rating": -2.8
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
