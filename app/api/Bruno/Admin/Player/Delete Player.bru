meta {
  name: Delete Player
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/admin/players/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: {{player3Id}}
}
