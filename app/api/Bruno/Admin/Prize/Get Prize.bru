meta {
  name: Get Prize
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/admin/prizes/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: {{prizeBookSetId}}
}
