meta {
  name: Delete Award
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/admin/awards/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: {{award3Id}}
}
